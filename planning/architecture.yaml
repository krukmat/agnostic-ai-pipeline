# Arquitectura:
Web: 
  Components: Servidor HTTP (Express.js)
  Datasources: No especificado, interactuarán directamente con los módulos del backend.
Backend:
  Frameworks: FastAPI (Python 3.8+)
  Decisiones ADRs:
    - Técnicas de manejo de datos sensibles (https://example.com/docs/adrs/data-handling.html).
Entidades (Productos, Usuarios):
- Usuario
- Producto
API endpoints (Métodos y Paths, Estandarizaciones según las normativas existentes)